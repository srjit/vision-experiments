
#include <iostream>
#include <opencv2/opencv.hpp>
#include <opencv2/core/eigen.hpp>
#include <opencv2/highgui.hpp>



void getCorrespondingPoint(int x, int y, cv::Mat fundamentalMatrix){

  double f11 = fundementalMatrix.at<double>(Point(0,0));
  double f12 = fundementalMatrix.at<double>(Point(1,0));
  double f13 = fundementalMatrix.at<double>(Point(2,0));

  double f21 = fundementalMatrix.at<double>(Point(0,1));
  double f22 = fundementalMatrix.at<double>(Point(1,1));
  double f23 = fundementalMatrix.at<double>(Point(2,1));

  double f31 = fundementalMatrix.at<double>(Point(0,2));
  double f32 = fundementalMatrix.at<double>(Point(1,2));
  double f33 = fundementalMatrix.at<double>(Point(2,2));    

  
  
  
}


void getDisparityMap(cv::Mat leftImage,
		     cv::Mat rightImage,
		     cv::Mat fundementalMatrix){

 /**
  *  For every pixel in the right image, find the pixel in the left image
  *
  */
  std::Mat<double> disparityMap;

  int width = rightImage.cols;
  int height = rightImage.rows;

  for(int i=0; i<height; i++){
    for(int j=0; j<width; j++){
      
      
    }
  }
  

}


